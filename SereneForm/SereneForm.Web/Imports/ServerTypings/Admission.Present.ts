namespace SereneForm.Admission {
    export enum Present {
        Present = 1,
        Absent = 2
    }
    Serenity.Decorators.registerEnumType(Present, 'SereneForm.Admission.Present', 'Admission.Gals');
}
