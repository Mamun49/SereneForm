namespace SereneForm.Admission {
    export enum ns1enum {
        Positive = 1,
        Negative = 2,
        NotDone = 3
    }
    Serenity.Decorators.registerEnumType(ns1enum, 'SereneForm.Admission.ns1enum', 'SereneForm.Admission.Forms.ns1enum');
}
